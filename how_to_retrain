#retraining the banknote-net:
conda activate py310
python ./src/train_custom.py  --> default folder is processed2-resized
python ./src/predict

open collab project. upload converter.py and h5 into collab. run cells with ###### encoder comment at start
download the tflite and rename and version it

add new model to pubspec, run flutter pub get.
modify model asset loader address and hardcoded address if any inside image pipeline
modify nn_classifier.dart and add new labels note that it should be sorted alphabetically
you must also check if there is a design record for that inference label in database

#retraining the yolo:
